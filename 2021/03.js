export function a(binaryNumbers) {
  let positions = [];

  for (let i = 0; i < binaryNumbers.length; i++) {
  // for (let i = 0; i < binaryNumbers.length; i++) {
    const number = binaryNumbers[i];
    // console.log(number);
  }

  // console.log(positions);

  let gammaRate = 0;
  let epsilonRate = 0;

  return gammaRate * epsilonRate;
}

export function b() {
}
